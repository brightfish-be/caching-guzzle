sudo: required

language: php

php:
  - 7.1
  - 7.2
  - 7.3

install:
  - travis_retry composer self-update
  - travis_retry composer update -prefer-dist --no-interaction --no-suggest

branches:
  only:
    - master
